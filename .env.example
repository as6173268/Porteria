# Supabase Configuration
# IMPORTANT: Never commit the actual .env file with real credentials
# These are example values - replace with your actual Supabase project credentials

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Security Notes:
# 1. The anon key is safe to expose in the client
# 2. Never expose your service_role key in the client
# 3. All sensitive operations should be handled server-side with RLS policies
# 4. Enable Row Level Security (RLS) on all tables
# 5. Use Supabase Auth for user authentication
# 6. Validate all inputs on both client and server side
